<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:OWCE.Views.Pages"
       x:Class="OWCE.AppShell"
       Shell.FlyoutBehavior="Disabled">

    <!-- Board list is the root / home page -->
    <ShellContent
        Title="Boards"
        ContentTemplate="{DataTemplate pages:BoardListPage}"
        Route="BoardList" />

</Shell>
